<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guia1_2022_prueba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Guia1_2022_Prueba">
        <activity
            android:name=".CalculadoraActivity"
            android:label="@string/msgCalc"/>
        <activity
            android:name=".LoginActivity"
            android:label="@string/msgForm_login"/>
        <activity
            android:name=".datos_alumno"
            android:label="@string/Dev" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Guia 01" />
    </application>

</manifest>